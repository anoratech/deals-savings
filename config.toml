languageCode = "en-us"
title = "SlickSavers"
[params]
prism_syntax_highlighting = true
  [params.prism]
    languages = [
      "markup",
      "css",
      "clike",
      "javascript"
    ]
    plugins = [
      "normalize-whitespace",
      "toolbar",
      "copy-to-clipboard"
    ]
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
